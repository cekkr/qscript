// 1. Initialize Register
// Two qubits, A and B. Initial state |00>
let pair = Register(2);

// 2. Expansion
// Spread Qubit 0's amplitude equally: |00> + |10>
pair.Superpose(targets: 0);

// 3. Pivot to entangle
// "If Qubit 0 is 1, Flip Qubit 1" to align the surviving realities.
pair.Flip(target: 1, where: pair[0] == 1);

// 4. Verify
// The only surviving outcomes are #00 and #11.
let output = pair.Measure();
