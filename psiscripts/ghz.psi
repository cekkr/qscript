// Build a 3-qubit GHZ state: (|000> + |111>) / sqrt(2). Scales to N-qubit GHZ/cluster states.
let ghz = Register(3);

// Seed the block on the first qubit
ghz.Superpose(targets: 0);

// Pivot the tag to the rest (controlled flips)
ghz.Flip(target: 1, where: ghz[0] == 1);
ghz.Flip(target: 2, where: ghz[0] == 1);

// Optional: observe the perfect three-way correlation
let outcome = ghz.Measure();
